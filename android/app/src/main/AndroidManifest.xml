<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools">
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
  <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
  <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
  <uses-permission android:name="android.permission.CAMERA" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.READ_PHONE_STATE" />

  <!-- Push notifications. Android 13 (API 33) made this a runtime permission;
       below 33 it is granted at install time and requestPermission() is a no-op. -->
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <!-- Saving downloads to public storage on Android <= 9 (API 28). API 29+ uses MediaStore, no permission needed. -->
  <uses-permission
    android:name="android.permission.WRITE_EXTERNAL_STORAGE"
    android:maxSdkVersion="28" />

  <!-- BLE device provisioning. Android 12+ (API 31) uses the new runtime BLUETOOTH_SCAN/CONNECT.
       neverForLocation tells the OS we don't derive physical location from BLE scans, so we don't
       need to force ACCESS_FINE_LOCATION on Android 12+. Legacy BLUETOOTH/BLUETOOTH_ADMIN cover <= API 30. -->
  <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
    android:usesPermissionFlags="neverForLocation" />
  <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
  <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
  <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
  <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

  <application
    android:name=".MainApplication"
    android:label="@string/app_name"
    android:icon="@mipmap/ic_launcher"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:allowBackup="false"
    android:theme="@style/BootTheme"
    android:usesCleartextTraffic="true"
    android:networkSecurityConfig="@xml/network_security_config"
    android:supportsRtl="true">
    <activity
      android:name=".MainActivity"
      android:label="@string/app_name"
      android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
      android:launchMode="singleTask"
      android:windowSoftInputMode="adjustResize"
      android:screenOrientation="portrait"
      android:exported="true"
      android:theme="@style/BootTheme">

    <!-- Normal app launch -->
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>

      <!-- Android App Link: https://app.smarteco.ai/SmartecoAvd/... -->
      <intent-filter android:autoVerify="true">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data
            android:scheme="https"
            android:host="app.smarteco.ai"
            android:pathPrefix="/SmartecoAvd" />
      </intent-filter>

    </activity>

    <!-- Defaults FCM applies when it draws a notification itself, i.e. whenever
         the app is backgrounded or killed.

         The channel is NAMED here but CREATED in JS (see
         src/services/pushNotifications.js). Android 8+ will not show a
         notification whose channel does not exist — it silently reroutes to an
         auto-created "Miscellaneous" channel — so this id, the one in
         pushNotifications.js, and `fcm_default_channel_id` on the API must all
         agree. -->
    <!-- tools:replace is required on the two below: @react-native-firebase/messaging
         declares both in its own library manifest (an empty channel id and
         @color/white), and the merger treats a differing value as a conflict
         rather than an override. -->
    <meta-data
      android:name="com.google.firebase.messaging.default_notification_channel_id"
      android:value="@string/default_notification_channel_id"
      tools:replace="android:value" />

    <!-- Must be a white-on-transparent silhouette. Android 5+ renders any
         coloured notification icon as a solid white square, which is why the
         launcher icon cannot be reused here. RNFirebase does not set a default
         icon, so this one needs no tools:replace. -->
    <meta-data
      android:name="com.google.firebase.messaging.default_notification_icon"
      android:resource="@drawable/ic_notification" />

    <meta-data
      android:name="com.google.firebase.messaging.default_notification_color"
      android:resource="@color/notification_color"
      tools:replace="android:resource" />

    <!-- No <service> declaration is needed: @react-native-firebase/messaging
         ships its own ReactNativeFirebaseMessagingService in its library
         manifest, which is merged in automatically. MainActivity is already
         launchMode="singleTask", which is what notification taps require. -->
  </application>
</manifest>
