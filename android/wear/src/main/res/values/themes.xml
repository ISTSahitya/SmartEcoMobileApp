<resources>
    <style name="Theme.SmartEcoWear" parent="@android:style/Theme.DeviceDefault">
        <item name="android:windowBackground">@color/background</item>
    </style>

    <!-- MainActivity uses this and calls installSplashScreen() before
         super.onCreate(), which then swaps to Theme.SmartEcoWear. -->
    <style name="Theme.SmartEcoWear.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.SmartEcoWear</item>
    </style>
</resources>
